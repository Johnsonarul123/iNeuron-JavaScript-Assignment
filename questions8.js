// 08. Demonstrate the use of trim() to remove any trailing whitespace at the beginning and the end of a string.
let newstr= "  I am a rockstar  "
let secstr = newstr.trim();

console.log(secstr);

// The trim() method is a built-in JavaScript method that can be called on a string. It removes any whitespace characters from the beginning and end of a string, and returns the new modified string. It can be used to remove any leading and trailing spaces, tabs or newlines from a string.